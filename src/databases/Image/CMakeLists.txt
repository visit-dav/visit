# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(Image_database)

ADD_DATABASE_CODE_GEN_TARGETS(Image SKIP_INFO)

SET(COMMON_SOURCES
ImagePluginInfo.C
ImageCommonPluginInfo.C
)

SET(LIBI_SOURCES
ImagePluginInfo.C
)

SET(LIBM_SOURCES
ImageMDServerPluginInfo.C
${COMMON_SOURCES}
avtImageFileFormat.C
avtImageOptions.C
)

SET(LIBE_SOURCES
ImageEnginePluginInfo.C
${COMMON_SOURCES}
vtkStimulateReader.C
avtImageFileFormat.C
avtImageOptions.C
avtImagePluginWriter.C
)

INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_DATABASE_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IImageDatabase ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IImageDatabase visitcommon)
SET(INSTALLTARGETS IImageDatabase)

IF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(MImageDatabase ${LIBM_SOURCES})
    TARGET_LINK_LIBRARIES(MImageDatabase visitcommon avtdbatts avtdatabase_ser )
    ADD_TARGET_DEFINITIONS(MImageDatabase MDSERVER)
    SET(INSTALLTARGETS ${INSTALLTARGETS} MImageDatabase)
ENDIF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EImageDatabase_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EImageDatabase_ser visitcommon avtpipeline_ser avtdatabase_ser )
ADD_TARGET_DEFINITIONS(EImageDatabase_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EImageDatabase_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EImageDatabase_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EImageDatabase_par visitcommon avtpipeline_par avtdatabase_par )
    ADD_TARGET_DEFINITIONS(EImageDatabase_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EImageDatabase_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_DATABASE_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_RTOD(databases ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(databases Image ${INSTALLTARGETS})

