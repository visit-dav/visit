# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(Xmdv_database)

ADD_DATABASE_CODE_GEN_TARGETS(Xmdv)

SET(COMMON_SOURCES
XmdvPluginInfo.C
XmdvCommonPluginInfo.C
)

SET(LIBI_SOURCES
XmdvPluginInfo.C
)

SET(LIBM_SOURCES
XmdvMDServerPluginInfo.C
${COMMON_SOURCES}
avtXmdvFileFormat.C
avtXmdvOptions.C
)

SET(LIBE_SOURCES
XmdvEnginePluginInfo.C
${COMMON_SOURCES}
avtXmdvFileFormat.C
avtXmdvWriter.C
avtXmdvOptions.C
)

INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_DATABASE_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IXmdvDatabase ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IXmdvDatabase visitcommon)
SET(INSTALLTARGETS IXmdvDatabase)

IF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(MXmdvDatabase ${LIBM_SOURCES})
    TARGET_LINK_LIBRARIES(MXmdvDatabase visitcommon avtdbatts avtdatabase_ser )
    ADD_TARGET_DEFINITIONS(MXmdvDatabase MDSERVER)
    SET(INSTALLTARGETS ${INSTALLTARGETS} MXmdvDatabase)
ENDIF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EXmdvDatabase_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EXmdvDatabase_ser visitcommon avtpipeline_ser avtdatabase_ser )
ADD_TARGET_DEFINITIONS(EXmdvDatabase_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EXmdvDatabase_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EXmdvDatabase_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EXmdvDatabase_par visitcommon avtpipeline_par avtdatabase_par )
    ADD_TARGET_DEFINITIONS(EXmdvDatabase_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EXmdvDatabase_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_DATABASE_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_OUTPUT_DIR(databases ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(databases Xmdv ${INSTALLTARGETS})

