# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(XYZ_database)

ADD_DATABASE_CODE_GEN_TARGETS(XYZ)

SET(COMMON_SOURCES
XYZPluginInfo.C
XYZCommonPluginInfo.C
)

SET(LIBI_SOURCES
XYZPluginInfo.C
)

SET(LIBM_SOURCES
XYZMDServerPluginInfo.C
${COMMON_SOURCES}
avtXYZFileFormat.C
)

SET(LIBE_SOURCES
XYZEnginePluginInfo.C
${COMMON_SOURCES}
avtXYZFileFormat.C
avtXYZWriter.C
)

INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_DATABASE_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IXYZDatabase ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IXYZDatabase visitcommon)
SET(INSTALLTARGETS IXYZDatabase)

IF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(MXYZDatabase ${LIBM_SOURCES})
    TARGET_LINK_LIBRARIES(MXYZDatabase visitcommon avtdbatts avtdatabase_ser )
    ADD_TARGET_DEFINITIONS(MXYZDatabase MDSERVER)
    SET(INSTALLTARGETS ${INSTALLTARGETS} MXYZDatabase)
ENDIF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EXYZDatabase_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EXYZDatabase_ser visitcommon avtpipeline_ser avtdatabase_ser )
ADD_TARGET_DEFINITIONS(EXYZDatabase_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EXYZDatabase_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EXYZDatabase_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EXYZDatabase_par visitcommon avtpipeline_par avtdatabase_par )
    ADD_TARGET_DEFINITIONS(EXYZDatabase_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EXYZDatabase_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_DATABASE_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_RTOD(databases ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(databases XYZ ${INSTALLTARGETS})

