# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(Gadget_database)

ADD_DATABASE_CODE_GEN_TARGETS(Gadget)

SET(COMMON_SOURCES
GadgetPluginInfo.C
GadgetCommonPluginInfo.C
)

SET(LIBI_SOURCES
GadgetPluginInfo.C
)

SET(LIBM_SOURCES
GadgetMDServerPluginInfo.C
${COMMON_SOURCES}
avtGadgetFileFormat.C
)

SET(LIBE_SOURCES
GadgetEnginePluginInfo.C
${COMMON_SOURCES}
avtGadgetFileFormat.C
)

INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_DATABASE_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IGadgetDatabase ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IGadgetDatabase visitcommon)
SET(INSTALLTARGETS IGadgetDatabase)

IF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(MGadgetDatabase ${LIBM_SOURCES})
    TARGET_LINK_LIBRARIES(MGadgetDatabase visitcommon avtdbatts avtdatabase_ser )
    ADD_TARGET_DEFINITIONS(MGadgetDatabase MDSERVER)
    SET(INSTALLTARGETS ${INSTALLTARGETS} MGadgetDatabase)
ENDIF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EGadgetDatabase_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EGadgetDatabase_ser visitcommon avtpipeline_ser avtdatabase_ser )
ADD_TARGET_DEFINITIONS(EGadgetDatabase_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EGadgetDatabase_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EGadgetDatabase_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EGadgetDatabase_par visitcommon avtpipeline_par avtdatabase_par )
    ADD_TARGET_DEFINITIONS(EGadgetDatabase_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EGadgetDatabase_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_DATABASE_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_RTOD(databases ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(databases Gadget ${INSTALLTARGETS})

