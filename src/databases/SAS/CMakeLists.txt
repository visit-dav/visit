# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(SAS_database)

ADD_DATABASE_CODE_GEN_TARGETS(SAS)

SET(COMMON_SOURCES
SASPluginInfo.C
SASCommonPluginInfo.C
)

SET(LIBI_SOURCES
SASPluginInfo.C
)

SET(LIBM_SOURCES
SASMDServerPluginInfo.C
${COMMON_SOURCES}
avtSASFileFormat.C
)

SET(LIBE_SOURCES
SASEnginePluginInfo.C
${COMMON_SOURCES}
avtSASFileFormat.C
)

INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_DATABASE_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(ISASDatabase ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(ISASDatabase visitcommon)
SET(INSTALLTARGETS ISASDatabase)

IF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(MSASDatabase ${LIBM_SOURCES})
    TARGET_LINK_LIBRARIES(MSASDatabase visitcommon avtdbatts avtdatabase_ser )
    ADD_TARGET_DEFINITIONS(MSASDatabase MDSERVER)
    SET(INSTALLTARGETS ${INSTALLTARGETS} MSASDatabase)
ENDIF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(ESASDatabase_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(ESASDatabase_ser visitcommon avtpipeline_ser avtdatabase_ser )
ADD_TARGET_DEFINITIONS(ESASDatabase_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} ESASDatabase_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(ESASDatabase_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(ESASDatabase_par visitcommon avtpipeline_par avtdatabase_par )
    ADD_TARGET_DEFINITIONS(ESASDatabase_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} ESASDatabase_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_DATABASE_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_OUTPUT_DIR(databases ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(databases SAS ${INSTALLTARGETS})

