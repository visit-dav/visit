# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(GDAL_database)

ADD_DATABASE_CODE_GEN_TARGETS(GDAL)

SET(COMMON_SOURCES
GDALPluginInfo.C
GDALCommonPluginInfo.C
)

SET(LIBI_SOURCES
GDALPluginInfo.C
)

SET(LIBM_SOURCES
GDALMDServerPluginInfo.C
${COMMON_SOURCES}
avtGDALFileFormat.C
)

SET(LIBE_SOURCES
GDALEnginePluginInfo.C
${COMMON_SOURCES}
avtGDALFileFormat.C
)

INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_DATABASE_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IGDALDatabase ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IGDALDatabase visitcommon)
SET(INSTALLTARGETS IGDALDatabase)

IF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(MGDALDatabase ${LIBM_SOURCES})
    TARGET_LINK_LIBRARIES(MGDALDatabase visitcommon avtdbatts avtdatabase_ser gdal )
    ADD_TARGET_DEFINITIONS(MGDALDatabase MDSERVER)
    SET(INSTALLTARGETS ${INSTALLTARGETS} MGDALDatabase)
ENDIF(NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EGDALDatabase_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EGDALDatabase_ser visitcommon avtpipeline_ser avtdatabase_ser gdal )
ADD_TARGET_DEFINITIONS(EGDALDatabase_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EGDALDatabase_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EGDALDatabase_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EGDALDatabase_par visitcommon avtpipeline_par avtdatabase_par gdal )
    ADD_TARGET_DEFINITIONS(EGDALDatabase_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EGDALDatabase_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_DATABASE_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_OUTPUT_DIR(databases ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(databases GDAL ${INSTALLTARGETS})

