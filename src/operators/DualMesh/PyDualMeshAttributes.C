// Copyright (c) Lawrence Livermore National Security, LLC and other VisIt
// Project developers.  See the top-level LICENSE file for dates and other
// details.  No copyright assignment is required to contribute to VisIt.

#include <PyDualMeshAttributes.h>
#include <ObserverToCallback.h>
#include <stdio.h>
#include <string.h>
#include <Py2and3Support.h>

// ****************************************************************************
// Module: PyDualMeshAttributes
//
// Purpose:
//   Atts for Dual Mesh Operator
//
// Note:       Autogenerated by xml2python. Do not modify by hand!
//
// Programmer: xml2python
// Creation:   omitted
//
// ****************************************************************************

//
// This struct contains the Python type information and a DualMeshAttributes.
//
struct PyDualMeshAttributesObject
{
    PyObject_HEAD
    DualMeshAttributes *data;
    bool        owns;
    PyObject   *parent;
};

//
// Internal prototypes
//
static PyObject *NewDualMeshAttributes(int);
std::string
PyDualMeshAttributes_ToString(const DualMeshAttributes *atts, const char *prefix, const bool forLogging)
{
    std::string str;
    char tmpStr[1000];

    const char *mode_names = "Auto, NodesToZones, ZonesToNodes";
    switch (atts->GetMode())
    {
      case DualMeshAttributes::Auto:
          snprintf(tmpStr, 1000, "%smode = %sAuto  # %s\n", prefix, prefix, mode_names);
          str += tmpStr;
          break;
      case DualMeshAttributes::NodesToZones:
          snprintf(tmpStr, 1000, "%smode = %sNodesToZones  # %s\n", prefix, prefix, mode_names);
          str += tmpStr;
          break;
      case DualMeshAttributes::ZonesToNodes:
          snprintf(tmpStr, 1000, "%smode = %sZonesToNodes  # %s\n", prefix, prefix, mode_names);
          str += tmpStr;
          break;
      default:
          break;
    }

    return str;
}

static PyObject *
DualMeshAttributes_Notify(PyObject *self, PyObject *args)
{
    PyDualMeshAttributesObject *obj = (PyDualMeshAttributesObject *)self;
    obj->data->Notify();
    Py_INCREF(Py_None);
    return Py_None;
}

static PyObject *
DualMeshAttributes_dir(PyObject *self, PyObject *args)
{
    static DualMeshAttributes atts; // dummy to access field names

    PyObject *dir_list = PyList_New(0);
    if (!dir_list)
    {
        PyErr_NoMemory();
        return NULL;
    }

    // Add methods from the methods table
    for (PyMethodDef const *method = &PyDualMeshAttributes_methods[0];
         method && method->ml_name;
         method++) {
        if (!strncmp(method->ml_name, "__dir__", 7)) continue;
        if (!strncmp(method->ml_name, "Notify", 6)) continue;
        PyList_Append(dir_list, PyUnicode_FromString(method->ml_name));
    }

    // Add members using generic AttributeGroup interface
    for (int i = 0; i < atts.NumAttributes(); i++) {
        PyList_Append(dir_list, PyUnicode_FromString(atts.GetFieldName(i).c_str()));
    }

    return dir_list;
}
/*static*/ PyObject *
DualMeshAttributes_SetMode(PyObject *self, PyObject *args)
{
    PyDualMeshAttributesObject *obj = (PyDualMeshAttributesObject *)self;

    PyObject *packaged_args = 0;

    // Handle args packaged into a tuple of size one
    // if we think the unpackaged args matches our needs
    if (PySequence_Check(args) && PySequence_Size(args) == 1)
    {
        packaged_args = PySequence_GetItem(args, 0);
        if (PyNumber_Check(packaged_args))
            args = packaged_args;
    }

    if (PySequence_Check(args))
    {
        Py_XDECREF(packaged_args);
        return PyErr_Format(PyExc_TypeError, "expecting a single number arg");
    }

    if (!PyNumber_Check(args))
    {
        Py_XDECREF(packaged_args);
        return PyErr_Format(PyExc_TypeError, "arg is not a number type");
    }

    long val = PyLong_AsLong(args);
    int cval = int(val);

    if ((val == -1 && PyErr_Occurred()) || long(cval) != val)
    {
        Py_XDECREF(packaged_args);
        PyErr_Clear();
        return PyErr_Format(PyExc_TypeError, "arg not interpretable as C++ int");
    }

    if (cval < 0 || cval >= 3)
    {
        std::stringstream ss;
        ss << "An invalid mode value was given." << std::endl;
        ss << "Valid values are in the range [0,2]." << std::endl;
        ss << "You can also use the following symbolic names:";
        ss << " Auto";
        ss << ", NodesToZones";
        ss << ", ZonesToNodes";
        return PyErr_Format(PyExc_ValueError, ss.str().c_str());
    }

    Py_XDECREF(packaged_args);

    // Set the mode in the object.
    obj->data->SetMode(DualMeshAttributes::ConversionMode(cval));

    Py_INCREF(Py_None);
    return Py_None;
}

/*static*/ PyObject *
DualMeshAttributes_GetMode(PyObject *self, PyObject *args)
{
    PyDualMeshAttributesObject *obj = (PyDualMeshAttributesObject *)self;
    PyObject *retval = PyInt_FromLong(long(obj->data->GetMode()));
    return retval;
}



PyMethodDef PyDualMeshAttributes_methods[DUALMESHATTRIBUTES_NMETH] = {
    {"__dir__", DualMeshAttributes_dir, METH_NOARGS},
    {"Notify", DualMeshAttributes_Notify, METH_NOARGS},
    {"SetMode", DualMeshAttributes_SetMode, METH_VARARGS},
    {"GetMode", DualMeshAttributes_GetMode, METH_VARARGS},
    {NULL, NULL}
};

//
// Type functions
//

static void
PyDualMeshAttributes_dealloc(PyObject *v)
{
   PyDualMeshAttributesObject *obj = (PyDualMeshAttributesObject *)v;
   if(obj->parent != 0)
       Py_DECREF(obj->parent);
   if(obj->owns)
       delete obj->data;
}

static PyObject *PyDualMeshAttributes_richcompare(PyObject *self, PyObject *other, int op);
PyObject *
PyDualMeshAttributes_getattro(PyObject *self, PyObject *attr_name)
{
    const char *name = PyUnicode_AsUTF8(attr_name);
    if (!name) return NULL;

    if(strcmp(name, "mode") == 0)
        return DualMeshAttributes_GetMode(self, NULL);
    if(strcmp(name, "Auto") == 0)
        return PyInt_FromLong(long(DualMeshAttributes::Auto));
    if(strcmp(name, "NodesToZones") == 0)
        return PyInt_FromLong(long(DualMeshAttributes::NodesToZones));
    if(strcmp(name, "ZonesToNodes") == 0)
        return PyInt_FromLong(long(DualMeshAttributes::ZonesToNodes));


    PyObject *meth = Py_FindMethod(PyDualMeshAttributes_methods, self, (char*)name);
    if (meth) return meth;

    return PyObject_GenericGetAttr(self, attr_name);
}

int
PyDualMeshAttributes_setattro(PyObject *self, PyObject *attr_name, PyObject *args)
{
    PyObject NULL_PY_OBJ;
    PyObject *obj = &NULL_PY_OBJ;
    const char *name = PyUnicode_AsUTF8(attr_name);
    if (!name) return -1;

    if(strcmp(name, "mode") == 0)
        obj = DualMeshAttributes_SetMode(self, args);
    else
        obj = PyInt_FromLong(PyObject_GenericSetAttr(self, attr_name, args));

    if (obj != NULL && obj != &NULL_PY_OBJ)
        Py_DECREF(obj);

    if (obj == &NULL_PY_OBJ)
    {
        obj = NULL;
        PyErr_Format(PyExc_NameError, "name '%s' is not defined", name);
    }
    else if (obj == NULL && !PyErr_Occurred())
        PyErr_Format(PyExc_RuntimeError, "unknown problem with '%s'", name);

    return (obj != NULL) ? 0 : -1;
}

PyObject *
PyDualMeshAttributes_str(PyObject *v)
{
    PyDualMeshAttributesObject *obj = (PyDualMeshAttributesObject *)v;
    return PyString_FromString(PyDualMeshAttributes_ToString(obj->data,"", false).c_str());
}

//
// The doc string for the class.
//
static char const *PyDualMeshAttributes_purpose = "Atts for Dual Mesh Operator";

//
// Initialize the python object type structure with default values.
// There is another version of this macro, VISIT_PY_TYPE_OBJ_CUSTOM,
// that allows for customization of the .tp_xxx slot methods. These
// macros are defined in src/visitpy/common/Py2and3Support.h
//
VISIT_PY_TYPE_OBJ_DEFAULT(DualMeshAttributes);

//
// Helper function for comparing.
//
static PyObject *
PyDualMeshAttributes_richcompare(PyObject *self, PyObject *other, int op)
{
    // only compare against the same type 
    if ( Py_TYPE(self) != &PyDualMeshAttributesType
         || Py_TYPE(other) != &PyDualMeshAttributesType)
    {
        Py_INCREF(Py_NotImplemented);
        return Py_NotImplemented;
    }

    PyObject *res = NULL;
    DualMeshAttributes *a = ((PyDualMeshAttributesObject *)self)->data;
    DualMeshAttributes *b = ((PyDualMeshAttributesObject *)other)->data;

    switch (op)
    {
       case Py_EQ:
           res = (*a == *b) ? Py_True : Py_False;
           break;
       case Py_NE:
           res = (*a != *b) ? Py_True : Py_False;
           break;
       default:
           res = Py_NotImplemented;
           break;
    }

    Py_INCREF(res);
    return res;
}

//
// Helper functions for object allocation.
//

static DualMeshAttributes *defaultAtts = 0;
static DualMeshAttributes *currentAtts = 0;

static PyObject *
NewDualMeshAttributes(int useCurrent)
{
    PyDualMeshAttributesObject *newObject;
    newObject = PyObject_NEW(PyDualMeshAttributesObject, &PyDualMeshAttributesType);
    if(newObject == NULL)
        return NULL;
    if(useCurrent && currentAtts != 0)
        newObject->data = new DualMeshAttributes(*currentAtts);
    else if(defaultAtts != 0)
        newObject->data = new DualMeshAttributes(*defaultAtts);
    else
        newObject->data = new DualMeshAttributes;
    newObject->owns = true;
    newObject->parent = 0;
    PyType_Ready(&PyDualMeshAttributesType);
    return (PyObject *)newObject;
}

static PyObject *
WrapDualMeshAttributes(const DualMeshAttributes *attr)
{
    PyDualMeshAttributesObject *newObject;
    newObject = PyObject_NEW(PyDualMeshAttributesObject, &PyDualMeshAttributesType);
    if(newObject == NULL)
        return NULL;
    newObject->data = (DualMeshAttributes *)attr;
    newObject->owns = false;
    newObject->parent = 0;
    return (PyObject *)newObject;
}

///////////////////////////////////////////////////////////////////////////////
//
// Interface that is exposed to the VisIt module.
//
///////////////////////////////////////////////////////////////////////////////

PyObject *
DualMeshAttributes_new(PyObject *self, PyObject *args)
{
    int useCurrent = 0;
    if (!PyArg_ParseTuple(args, "i", &useCurrent))
    {
        if (!PyArg_ParseTuple(args, ""))
            return NULL;
        else
            PyErr_Clear();
    }

    return (PyObject *)NewDualMeshAttributes(useCurrent);
}

//
// Plugin method table. These methods are added to the visitmodule's methods.
//
static PyMethodDef DualMeshAttributesMethods[] = {
    {"DualMeshAttributes", DualMeshAttributes_new, METH_VARARGS},
    {NULL,      NULL}        /* Sentinel */
};

static Observer *DualMeshAttributesObserver = 0;

std::string
PyDualMeshAttributes_GetLogString()
{
    std::string s("DualMeshAtts = DualMeshAttributes()\n");
    if(currentAtts != 0)
        s += PyDualMeshAttributes_ToString(currentAtts, "DualMeshAtts.", true);
    return s;
}

static void
PyDualMeshAttributes_CallLogRoutine(Subject *subj, void *data)
{
    typedef void (*logCallback)(const std::string &);
    logCallback cb = (logCallback)data;

    if(cb != 0)
    {
        std::string s("DualMeshAtts = DualMeshAttributes()\n");
        s += PyDualMeshAttributes_ToString(currentAtts, "DualMeshAtts.", true);
        cb(s);
    }
}

void
PyDualMeshAttributes_StartUp(DualMeshAttributes *subj, void *data)
{
    if(subj == 0)
        return;

    currentAtts = subj;
    PyDualMeshAttributes_SetDefaults(subj);

    //
    // Create the observer that will be notified when the attributes change.
    //
    if(DualMeshAttributesObserver == 0)
    {
        DualMeshAttributesObserver = new ObserverToCallback(subj,
            PyDualMeshAttributes_CallLogRoutine, (void *)data);
    }

}

void
PyDualMeshAttributes_CloseDown()
{
    delete defaultAtts;
    defaultAtts = 0;
    delete DualMeshAttributesObserver;
    DualMeshAttributesObserver = 0;
}

PyMethodDef *
PyDualMeshAttributes_GetMethodTable(int *nMethods)
{
    *nMethods = 1;
    return DualMeshAttributesMethods;
}

bool
PyDualMeshAttributes_Check(PyObject *obj)
{
    return (obj->ob_type == &PyDualMeshAttributesType);
}

DualMeshAttributes *
PyDualMeshAttributes_FromPyObject(PyObject *obj)
{
    PyDualMeshAttributesObject *obj2 = (PyDualMeshAttributesObject *)obj;
    return obj2->data;
}

PyObject *
PyDualMeshAttributes_New()
{
    return NewDualMeshAttributes(0);
}

PyObject *
PyDualMeshAttributes_Wrap(const DualMeshAttributes *attr)
{
    return WrapDualMeshAttributes(attr);
}

void
PyDualMeshAttributes_SetParent(PyObject *obj, PyObject *parent)
{
    PyDualMeshAttributesObject *obj2 = (PyDualMeshAttributesObject *)obj;
    obj2->parent = parent;
}

void
PyDualMeshAttributes_SetDefaults(const DualMeshAttributes *atts)
{
    if(defaultAtts)
        delete defaultAtts;

    defaultAtts = new DualMeshAttributes(*atts);
}

