# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(Contour_plot)

ADD_PLOT_CODE_GEN_TARGETS(Contour)

SET(COMMON_SOURCES
ContourPluginInfo.C
ContourCommonPluginInfo.C
ContourAttributes.C
)

SET(LIBI_SOURCES
ContourPluginInfo.C
)

SET(LIBG_SOURCES
ContourGUIPluginInfo.C
QvisContourPlotWindow.C
${COMMON_SOURCES}
)
SET(LIBG_MOC_SOURCES
QvisContourPlotWindow.h
)

SET(LIBV_SOURCES
ContourViewerEnginePluginInfo.C
ContourViewerPluginInfo.C
avtContourPlot.C
${COMMON_SOURCES}
)

SET(LIBE_SOURCES
ContourViewerEnginePluginInfo.C
ContourEnginePluginInfo.C
avtContourPlot.C
${COMMON_SOURCES}
)


INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_PLOT_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IContourPlot ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IContourPlot visitcommon)
SET(INSTALLTARGETS IContourPlot)

IF(NOT VISIT_SERVER_COMPONENTS_ONLY AND NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    QT_WRAP_CPP(GContourPlot LIBG_SOURCES ${LIBG_MOC_SOURCES})
    ADD_LIBRARY(GContourPlot ${LIBG_SOURCES})
    TARGET_LINK_LIBRARIES(GContourPlot visitcommon gui )

    ADD_LIBRARY(VContourPlot ${LIBV_SOURCES})
    ADD_TARGET_DEFINITIONS(VContourPlot VIEWER)
    TARGET_LINK_LIBRARIES(VContourPlot visitcommon viewer )

    SET(INSTALLTARGETS ${INSTALLTARGETS} GContourPlot VContourPlot)

    IF(VISIT_PYTHON_SCRIPTING)
        SET(LIBS_SOURCES
            ContourScriptingPluginInfo.C
            PyContourAttributes.C
            ${COMMON_SOURCES}
        )
        ADD_LIBRARY(SContourPlot ${LIBS_SOURCES})
        IF(WIN32)
            # This prevents python from #defining snprintf as _snprintf
            SET_TARGET_PROPERTIES(SContourPlot PROPERTIES COMPILE_DEFINITIONS HAVE_SNPRINTF)
        ENDIF()
        TARGET_LINK_LIBRARIES(SContourPlot visitcommon visitpy ${PYTHON_LIBRARY})
        SET(INSTALLTARGETS ${INSTALLTARGETS} SContourPlot)
    ENDIF(VISIT_PYTHON_SCRIPTING)

    IF(VISIT_JAVA)
        FILE(COPY ContourAttributes.java DESTINATION ${JavaClient_BINARY_DIR}/src/plots)
        ADD_CUSTOM_TARGET(JavaContour ALL ${Java_JAVAC_EXECUTABLE} ${VISIT_Java_FLAGS} -d ${JavaClient_BINARY_DIR} -classpath ${JavaClient_BINARY_DIR} -sourcepath ${JavaClient_BINARY_DIR} ContourAttributes.java
            DEPENDS JavaClient
            WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})
    ENDIF(VISIT_JAVA)
ENDIF(NOT VISIT_SERVER_COMPONENTS_ONLY AND NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EContourPlot_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EContourPlot_ser visitcommon avtpipeline_ser avtplotter_ser )
ADD_TARGET_DEFINITIONS(EContourPlot_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EContourPlot_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EContourPlot_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EContourPlot_par visitcommon avtpipeline_par avtplotter_par )
    ADD_TARGET_DEFINITIONS(EContourPlot_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EContourPlot_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_PLOT_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_RTOD(plots ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(plots Contour ${INSTALLTARGETS})

