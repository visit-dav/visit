# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(Vector_plot)

ADD_PLOT_CODE_GEN_TARGETS(Vector)

SET(COMMON_SOURCES
VectorPluginInfo.C
VectorCommonPluginInfo.C
VectorAttributes.C
)

SET(LIBI_SOURCES
VectorPluginInfo.C
)

SET(LIBG_SOURCES
VectorGUIPluginInfo.C
QvisVectorPlotWindow.C
${COMMON_SOURCES}
)

SET(LIBV_SOURCES
VectorViewerEnginePluginInfo.C
VectorViewerPluginInfo.C
avtVectorPlot.C
avtVectorFilter.C
${COMMON_SOURCES}
)

SET(LIBE_SOURCES
VectorViewerEnginePluginInfo.C
VectorEnginePluginInfo.C
avtVectorPlot.C
avtVectorFilter.C
${COMMON_SOURCES}
)


INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_PLOT_INCLUDES}
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IVectorPlot ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IVectorPlot visitcommon)
SET(INSTALLTARGETS IVectorPlot)

IF(NOT VISIT_SERVER_COMPONENTS_ONLY AND NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    ADD_LIBRARY(GVectorPlot ${LIBG_SOURCES})
    set_target_properties(GVectorPlot PROPERTIES AUTOMOC ON)
    TARGET_LINK_LIBRARIES(GVectorPlot visitcommon gui )

    ADD_LIBRARY(VVectorPlot ${LIBV_SOURCES})
    ADD_TARGET_DEFINITIONS(VVectorPlot VIEWER)
    TARGET_LINK_LIBRARIES(VVectorPlot visitcommon viewer )

    SET(INSTALLTARGETS ${INSTALLTARGETS} GVectorPlot VVectorPlot)

    IF(VISIT_PYTHON_SCRIPTING)
        SET(LIBS_SOURCES
            VectorScriptingPluginInfo.C
            PyVectorAttributes.C
            ${COMMON_SOURCES}
        )
        ADD_LIBRARY(SVectorPlot ${LIBS_SOURCES})
        IF(WIN32)
            # This prevents python from #defining snprintf as _snprintf
            SET_TARGET_PROPERTIES(SVectorPlot PROPERTIES COMPILE_DEFINITIONS HAVE_SNPRINTF)
        ENDIF()
        TARGET_LINK_LIBRARIES(SVectorPlot visitcommon visitpy ${PYTHON_LIBRARY})
        SET(INSTALLTARGETS ${INSTALLTARGETS} SVectorPlot)
    ENDIF(VISIT_PYTHON_SCRIPTING)

    IF(VISIT_JAVA)
        FILE(COPY VectorAttributes.java DESTINATION ${JavaClient_BINARY_DIR}/src/plots)
        ADD_CUSTOM_TARGET(JavaVector ALL ${Java_JAVAC_EXECUTABLE} ${VISIT_Java_FLAGS} -d ${JavaClient_BINARY_DIR} -classpath ${JavaClient_BINARY_DIR} -sourcepath ${JavaClient_BINARY_DIR} VectorAttributes.java
            DEPENDS JavaClient
            WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})
    ENDIF(VISIT_JAVA)
ENDIF(NOT VISIT_SERVER_COMPONENTS_ONLY AND NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EVectorPlot_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EVectorPlot_ser visitcommon avtpipeline_ser avtplotter_ser )
ADD_TARGET_DEFINITIONS(EVectorPlot_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EVectorPlot_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EVectorPlot_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EVectorPlot_par visitcommon avtpipeline_par avtplotter_par )
    ADD_TARGET_DEFINITIONS(EVectorPlot_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EVectorPlot_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_PLOT_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_OUTPUT_DIR(plots ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(plots Vector ${INSTALLTARGETS})

