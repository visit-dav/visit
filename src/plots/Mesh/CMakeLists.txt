# DO NOT EDIT THIS FILE! THIS FILE IS AUTOMATICALLY GENERATED BY xml2cmake
PROJECT(Mesh_plot)

ADD_PLOT_CODE_GEN_TARGETS(Mesh)

SET(COMMON_SOURCES
MeshPluginInfo.C
MeshCommonPluginInfo.C
MeshAttributes.C
)

SET(LIBI_SOURCES
MeshPluginInfo.C
)

SET(LIBG_SOURCES
MeshGUIPluginInfo.C
QvisMeshPlotWindow.C
${COMMON_SOURCES}
)
SET(LIBG_MOC_SOURCES
QvisMeshPlotWindow.h
)

SET(LIBV_SOURCES
MeshViewerEnginePluginInfo.C
MeshViewerPluginInfo.C
avtMeshPlot.C
avtMeshFilter.C
avtMeshPlotMapper.C
${COMMON_SOURCES}
)

SET(LIBE_SOURCES
MeshViewerEnginePluginInfo.C
MeshEnginePluginInfo.C
avtMeshPlot.C
avtMeshFilter.C
avtMeshPlotMapper.C
${COMMON_SOURCES}
)


INCLUDE_DIRECTORIES(
${CMAKE_CURRENT_SOURCE_DIR}
${VISIT_PLOT_INCLUDES}
${VISIT_INCLUDE_DIR}/avt/Plotter/vtk
)

LINK_DIRECTORIES(${VISIT_LIBRARY_DIR} )

ADD_LIBRARY(IMeshPlot ${LIBI_SOURCES})
TARGET_LINK_LIBRARIES(IMeshPlot visitcommon)
SET(INSTALLTARGETS IMeshPlot)

IF(NOT VISIT_SERVER_COMPONENTS_ONLY AND NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)
    QT_WRAP_CPP(GMeshPlot LIBG_SOURCES ${LIBG_MOC_SOURCES})
    ADD_LIBRARY(GMeshPlot ${LIBG_SOURCES})
    TARGET_LINK_LIBRARIES(GMeshPlot visitcommon gui )

    ADD_LIBRARY(VMeshPlot ${LIBV_SOURCES})
    ADD_TARGET_DEFINITIONS(VMeshPlot VIEWER)
    TARGET_LINK_LIBRARIES(VMeshPlot visitcommon viewer )

    SET(INSTALLTARGETS ${INSTALLTARGETS} GMeshPlot VMeshPlot)

    IF(VISIT_PYTHON_SCRIPTING)
        SET(LIBS_SOURCES
            MeshScriptingPluginInfo.C
            PyMeshAttributes.C
            ${COMMON_SOURCES}
        )
        ADD_LIBRARY(SMeshPlot ${LIBS_SOURCES})
        IF(WIN32)
            # This prevents python from #defining snprintf as _snprintf
            SET_TARGET_PROPERTIES(SMeshPlot PROPERTIES COMPILE_DEFINITIONS HAVE_SNPRINTF)
        ENDIF()
        TARGET_LINK_LIBRARIES(SMeshPlot visitcommon visitpy ${PYTHON_LIBRARY})
        SET(INSTALLTARGETS ${INSTALLTARGETS} SMeshPlot)
    ENDIF(VISIT_PYTHON_SCRIPTING)

    IF(VISIT_JAVA)
        FILE(COPY MeshAttributes.java DESTINATION ${JavaClient_BINARY_DIR}/src/plots)
        ADD_CUSTOM_TARGET(JavaMesh ALL ${Java_JAVAC_EXECUTABLE} ${VISIT_Java_FLAGS} -d ${JavaClient_BINARY_DIR} -classpath ${JavaClient_BINARY_DIR} -sourcepath ${JavaClient_BINARY_DIR} MeshAttributes.java
            DEPENDS JavaClient
            WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})
    ENDIF(VISIT_JAVA)
ENDIF(NOT VISIT_SERVER_COMPONENTS_ONLY AND NOT VISIT_ENGINE_ONLY AND NOT VISIT_DBIO_ONLY)

ADD_LIBRARY(EMeshPlot_ser ${LIBE_SOURCES})
TARGET_LINK_LIBRARIES(EMeshPlot_ser visitcommon avtpipeline_ser avtplotter_ser )
ADD_TARGET_DEFINITIONS(EMeshPlot_ser ENGINE)
SET(INSTALLTARGETS ${INSTALLTARGETS} EMeshPlot_ser)

IF(VISIT_PARALLEL)
    ADD_PARALLEL_LIBRARY(EMeshPlot_par ${LIBE_SOURCES})
    TARGET_LINK_LIBRARIES(EMeshPlot_par visitcommon avtpipeline_par avtplotter_par )
    ADD_TARGET_DEFINITIONS(EMeshPlot_par ENGINE)
    SET(INSTALLTARGETS ${INSTALLTARGETS} EMeshPlot_par)
ENDIF(VISIT_PARALLEL)

VISIT_INSTALL_PLOT_PLUGINS(${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_RTOD(plots ${INSTALLTARGETS})
VISIT_PLUGIN_TARGET_FOLDER(plots Mesh ${INSTALLTARGETS})

